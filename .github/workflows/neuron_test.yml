name: Basic Test
on: pull_request

permissions:
  contents: read
  actions: write
  checks: write
  pull-requests: write

jobs:
  test:
    name: Basic Test
    runs-on: self-hosted
    timeout-minutes: 5  # Add timeout
    steps:
      - uses: actions/checkout@v2
      
      - name: Simple Test
        run: |
          echo "Hello from runner"
          exit 0  # Explicitly exit