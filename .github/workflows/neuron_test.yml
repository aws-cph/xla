name: Basic Test
on: pull_request

jobs:
  test:
    runs-on: self-hosted
    timeout-minutes: 5  # Add timeout
    steps:
      - name: Simple Echo
        run: |
          echo "Hello from runner"
          echo "Current time: $(date)"
          echo "Current directory: $(pwd)"